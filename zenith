local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
    Name = "Zenith Inc.",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "Drac's Creation",
    ConfigurationSaving = {
        Enabled = false
    }
})

local Tab = Window:CreateTab("Main", 4483362458)

local ReplicaID = 271
local Event = game:GetService("ReplicatedStorage")
    .ReplicaRemoteEvents
    .Replica_ReplicaSignal

local AutoUpgrades = false
local AutoRebirth = false
local AutoAscension = false

Tab:CreateInput({
    Name = "Replica ID",
    PlaceholderText = "271",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        local Num = tonumber(Value)
        if Num then
            ReplicaID = Num
        end
    end
})

Tab:CreateToggle({
    Name = "Auto Mejoras (cada 25s)",
    CurrentValue = false,
    Flag = "AutoUpgrades",
    Callback = function(Value)
        AutoUpgrades = Value

        if Value then
            task.spawn(function()
                while AutoUpgrades do
                    for i = 1, 45 do
                        if not AutoUpgrades then
                            break
                        end

                        Event:FireServer(
                            ReplicaID,
                            "RequestUpgrade",
                            "ground_upg" .. i,
                            "max"
                        )

                        task.wait(0.25)
                    end

                    local t = 25
                    while AutoUpgrades and t > 0 do
                        task.wait(1)
                        t -= 1
                    end
                end
            end)
        end
    end
})

Tab:CreateToggle({
    Name = "Auto Rebirth (cada 3 mins)",
    CurrentValue = false,
    Flag = "AutoRebirth",
    Callback = function(Value)
        AutoRebirth = Value

        if Value then
            task.spawn(function()
                while AutoRebirth do
                    Event:FireServer(
                        ReplicaID,
                        "RequestPrestige",
                        "Rebirth"
                    )

                    local t = 180
                    while AutoRebirth and t > 0 do
                        task.wait(1)
                        t -= 1
                    end
                end
            end)
        end
    end
})

Tab:CreateToggle({
    Name = "Auto Ascension (cada 10s)",
    CurrentValue = false,
    Flag = "AutoAscension",
    Callback = function(Value)
        AutoAscension = Value

        if Value then
            task.spawn(function()
                local Event = game:GetService("ReplicatedStorage")
                    .ReplicaRemoteEvents
                    .Replica_ReplicaSignal

                while AutoAscension do
                    Event:FireServer(
                        ReplicaID,
                        "RequestAscension"
                    )

                    local t = 10
                    while AutoAscension and t > 0 do
                        task.wait(1)
                        t -= 1
                    end
                end
            end)
        end
    end
})

Tab:CreateButton({
    Name = "Buy Temple Tickets",
    Callback = function()
        local Players = game:GetService("Players")
        local PathfindingService = game:GetService("PathfindingService")

        local player = Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:WaitForChild("Humanoid")
        local root = character:WaitForChild("HumanoidRootPart")

        local destination = Vector3.new(65, 122, -1117)

        local path = PathfindingService:CreatePath({
            AgentRadius = 2,
            AgentHeight = 5,
            AgentCanJump = true,
            AgentJumpHeight = 7,
            AgentMaxSlope = 45
        })

        path:ComputeAsync(root.Position, destination)

        if path.Status == Enum.PathStatus.Success then
            for _, waypoint in ipairs(path:GetWaypoints()) do
                if waypoint.Action == Enum.PathWaypointAction.Jump then
                    humanoid.Jump = true
                end

                humanoid:MoveTo(waypoint.Position)

                local reached = humanoid.MoveToFinished:Wait()
                if not reached then
                    break
                end
            end
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "No se pudo calcular una ruta.",
                Duration = 3
            })
        end
    end
})

Tab:CreateButton({
    Name = "Roll Temple Tickets",
    Callback = function()
        local Players = game:GetService("Players")
        local PathfindingService = game:GetService("PathfindingService")

        local player = Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:WaitForChild("Humanoid")
        local root = character:WaitForChild("HumanoidRootPart")

        local destination = Vector3.new(116, 124, -1214)

        local path = PathfindingService:CreatePath({
            AgentRadius = 2,
            AgentHeight = 5,
            AgentCanJump = true,
            AgentJumpHeight = 7,
            AgentMaxSlope = 45
        })

        path:ComputeAsync(root.Position, destination)

        if path.Status == Enum.PathStatus.Success then
            for _, waypoint in ipairs(path:GetWaypoints()) do
                if waypoint.Action == Enum.PathWaypointAction.Jump then
                    humanoid.Jump = true
                end

                humanoid:MoveTo(waypoint.Position)

                local reached = humanoid.MoveToFinished:Wait()
                if not reached then
                    break
                end
            end
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "No se pudo calcular una ruta.",
                Duration = 3
            })
        end
    end
})

Tab:CreateButton({
    Name = "Rune Ascension Roll",
    Callback = function()
        local Players = game:GetService("Players")
        local PathfindingService = game:GetService("PathfindingService")

        local player = Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:WaitForChild("Humanoid")
        local root = character:WaitForChild("HumanoidRootPart")

        local destination = Vector3.new(143, 292.5, -1942)

        local path = PathfindingService:CreatePath({
            AgentRadius = 2,
            AgentHeight = 5,
            AgentCanJump = true,
            AgentJumpHeight = 7,
            AgentMaxSlope = 45
        })

        path:ComputeAsync(root.Position, destination)

        if path.Status == Enum.PathStatus.Success then
            for _, waypoint in ipairs(path:GetWaypoints()) do
                if waypoint.Action == Enum.PathWaypointAction.Jump then
                    humanoid.Jump = true
                end

                humanoid:MoveTo(waypoint.Position)

                local reached = humanoid.MoveToFinished:Wait()
                if not reached then
                    break
                end
            end
        else
            Rayfield:Notify({
                Title = "Error",
                Content = "No se pudo calcular una ruta.",
                Duration = 3
            })
        end
    end
})
